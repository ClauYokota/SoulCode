import { BntDirective } from './bnt.directive';

describe('BntDirective', () => {
  it('should create an instance', () => {
    const directive = new BntDirective();
    expect(directive).toBeTruthy();
  });
});
